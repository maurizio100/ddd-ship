apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: ddd-ship
data:
  SPRING_PROFILES_ACTIVE: "dev"
  SPRING_DATASOURCE_URL: "jdbc:postgresql://postgres:5432/hexagonship"
  MINIO_URL: "http://minio:9000"
  MINIO_BUCKET_NAME: "catains"

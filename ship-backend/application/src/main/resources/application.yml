spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hexagonship
    username: hexagonship_user
    password: hexagonship_user
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL81Dialect